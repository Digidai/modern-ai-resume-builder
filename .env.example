# Optional: provide a Gemini key for local development.
# WARNING: variables prefixed with VITE_ are embedded into the client bundle.
VITE_GEMINI_API_KEY=

# Optional: canonical site URL for SEO assets (sitemap, canonical, OG URLs).
# Prefer SITE_URL to avoid embedding into the client bundle.
SITE_URL=https://moderncv.pages.dev
